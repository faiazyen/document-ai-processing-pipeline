# Next Steps

This folder is ready to serve as the planning source for the new project.

## Do Not Do Yet

- Do not initialize the Next.js app yet.
- Do not install dependencies yet.
- Do not create or push a GitHub repo yet.
- Do not deploy to Vercel yet.
- Do not add real API keys to files.

## Suggested Next Session

When ready to start implementation:

1. Confirm whether the project should be created inside this folder or in a separate `document-ai-processing-pipeline` repo folder.
2. Initialize the Next.js project.
3. Read the installed Next.js documentation before writing code, per local agent rules.
4. Add the planned folder structure.
5. Add `.env.example`.
6. Build the UI shell.
7. Add invoice schema and validation tests.
8. Add document extraction and OpenAI integration.
9. Run local verification.
10. Only later, create GitHub repo and Vercel deployment.

## Implementation Reminder

This planning step intentionally stopped before project execution. The next build phase should begin from this folder's plan and should keep secrets out of source control.

